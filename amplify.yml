version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build commands needed"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
    discard:
      - 'node_modules/**/*'
      - '.git/**/*'